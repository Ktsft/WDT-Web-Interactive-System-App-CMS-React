import * as React from 'react';
import type { Toast as T } from '../types';
declare const Toast: ({ toast, opened, setOpened }: {
    toast: T;
    opened: boolean;
    setOpened: React.Dispatch<React.SetStateAction<boolean>>;
}) => JSX.Element;
export default Toast;
