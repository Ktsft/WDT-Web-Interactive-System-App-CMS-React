import { Toast } from '../types';
declare const ToastContent: ({ toast, onClick, visible, showProgressBar, progress }: {
    toast: Toast;
    onClick?: (() => void) | undefined;
    visible: boolean;
    showProgressBar: boolean;
    progress: number;
}) => JSX.Element;
export default ToastContent;
